[defaults]
inventory = inventory/hosts.ini
roles_path = roles
host_key_checking = False
deprecation_warnings = False
# ssh reads ~/.ssh/config by default (brev writes host aliases there).
[ssh_connection]
ssh_args = -o ControlMaster=auto -o ControlPersist=60s
pipelining = True
